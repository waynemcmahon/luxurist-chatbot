{
  "framework": "nextjs",
  "buildCommand": "npm run build && npm run build:webflow",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "public": true,
  "rewrites": [
    {
      "source": "/dist/:path*",
      "destination": "/dist/:path*"
    }
  ]
} 